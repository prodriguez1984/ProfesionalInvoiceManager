<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ort.profesionalinvoicemanager.views">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icono_facturacion"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icono_facturacion_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.products.ProductCreate"
            android:label="@string/title_activity_product_create"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".ui.products.ProductInspection" />
        <activity android:name=".TaxActivity" />
        <activity
            android:name=".IndustryActivity"
            android:label="@string/title_activity_industry"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".IndustryFragment"
            android:label="@string/title_activity_industry"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SignUpActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/apiKey" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

</manifest>